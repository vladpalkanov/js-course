import React from 'react'
import { useParams, useQuery } from 'react-router-dom'

export const UserDetailPage = props => {
  const params = useParams()
  // const query = useQuery()
  return <div>UserDetailPage works</div>
}